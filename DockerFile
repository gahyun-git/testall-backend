# Python 3.11-slim 베이스 이미지 사용
FROM python:3.11-slim

# 파이썬에서 pyc 파일 생성 방지 및 버퍼 없이 로그 출력
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# 작업 디렉토리 생성
WORKDIR /app

# 시스템 의존성 설치 (예: gcc, libpq-dev 등 PostgreSQL 관련 빌드 도구)
RUN apt-get update && apt-get install -y gcc libpq-dev && apt-get clean

# requirements.txt 복사 후 의존성 설치
COPY requirements.txt /app/
RUN pip install --upgrade pip && pip install --no-cache-dir -r requirements.txt

# 프로젝트 전체 복사 (Docker Compose에서 마운트되는 경우 코드 변경 사항이 바로 반영됩니다)
COPY . /app/

# 포트 8000 노출 (Django 기본 포트)
EXPOSE 8000

# 개발용 Django 서버 실행 (실제 배포 시 Gunicorn 등으로 변경할 수 있음)
CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
